  #include <iostream>
  #include "Programa.h"
  using namespace std;
  
  int main(){
      Programa programa;
      programa.Bienvenida();
      return 0;
}
/*Nodo* inventario = NULL;
      Coordinador c("HombreNiño");
        
      inventario = c.addArticulo(inventario);
      inventario = c.addArticulo(inventario);
      c.verLista(inventario);
      c.modArticulo(inventario, 2);
    
      Cliente d("caca");
      d.comprar(inventario);
      c.verLista(inventario);
      d.pagar(inventario);
      c.verLista(inventario);*/